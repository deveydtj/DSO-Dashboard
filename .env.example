# Example environment configuration
# Copy this file to .env and fill in your GitLab credentials

# GitLab API Configuration
GITLAB_URL=https://gitlab.com
GITLAB_API_TOKEN=your_gitlab_api_token_here

# Server Configuration
PORT=8080
POLL_INTERVAL=60
CACHE_TTL=300

# Pagination Configuration
# PER_PAGE controls how many items are fetched per API request
# Default is 100 (GitLab's typical maximum)
# The dashboard automatically fetches all pages, so groups with hundreds
# of projects will load completely regardless of this setting
# PER_PAGE=100

# Mock Data Mode (optional)
# Set to 'true' to run without GitLab API access using mock_data.json
# USE_MOCK_DATA=false

# Mock Scenario Selection (optional, only used when USE_MOCK_DATA=true)
# Available scenarios: healthy, failing, running
# Leave empty or unset to use default mock_data.json
# MOCK_SCENARIO=healthy

# Notes:
# - GITLAB_URL: Your GitLab instance URL (default: https://gitlab.com)
# - GITLAB_API_TOKEN: Your GitLab personal access token with API read permissions
# - PORT: Port for the HTTP server (default: 8080)
# - POLL_INTERVAL: Background polling interval in seconds (default: 60)
# - CACHE_TTL: Cache time-to-live in seconds (default: 300 = 5 minutes)
# - PER_PAGE: Items per API request page (default: 100)
# - USE_MOCK_DATA: Set to 'true' to use mock data instead of GitLab API (default: false)
# - MOCK_SCENARIO: Choose 'healthy', 'failing', or 'running' scenario (requires USE_MOCK_DATA=true)
